{
  "name": "@yarn/shared",
  "version": "1.0.0",
  "description": "Shared types, constants, and utilities for Yarn monorepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit",
    "prepare": "npm run build"
  },
  "keywords": [
    "shared",
    "types",
    "utilities"
  ],
  "author": "Yarn Team",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^5.0.7",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "tslib": "^2.6.3"
  }
}
